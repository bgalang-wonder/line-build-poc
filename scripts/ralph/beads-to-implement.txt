# Beads to Implement - P0 Completion Sprint
# Generated: Wed Jan  8 2026
# Focus: Wire existing components into editor UI
#
# CONTEXT: Components exist but are NOT mounted in EditorContainer.
# GPT 5.2 audit revealed StepEditor, BOMAutocomplete, DependenciesMultiSelect
# are imported but never rendered. This sprint wires them up.

# === P0 CRITICAL PATH (Wire UI Components) ===
# These 4 beads move us from ~60% to ~75% completion

benchtop-l5c    # Wire StepEditor into EditorContainer (HIGHEST PRIORITY)
benchtop-tp2    # Wire BOMAutocomplete into StepEditor target field
benchtop-0m6    # Wire DependenciesMultiSelect into StepEditor
benchtop-rnq    # Display complexity score in editor UI

# === P0 EPICS TO CLOSE (after children done) ===
# Check child tasks with: bd dep tree <epic-id>
# Close when all children are closed

benchtop-x0c.3  # Epic: Form Panel - close after l5c, tp2, 0m6
benchtop-x0c.1  # Epic: Project Setup - all children done
benchtop-x0c.2  # Epic: Data Layer - all children done
benchtop-x0c.5  # Epic: Chat Panel - mostly done
benchtop-x0c.6  # Epic: Validation Panel - all children done
benchtop-x0c.7  # Epic: Validation Engine - all children done
benchtop-x0c.11 # Epic: App Shell - all children done
