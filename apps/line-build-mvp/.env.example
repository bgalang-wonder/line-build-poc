# Vertex AI Configuration
# Required: Your GCP project ID
VERTEX_AI_PROJECT=your-gcp-project-id

# Optional: Region (defaults to us-central1)
VERTEX_AI_LOCATION=us-central1

# Optional: Model name (defaults to gemini-2.5-flash)
VERTEX_AI_MODEL=gemini-2.5-flash

# Authentication: Set GOOGLE_APPLICATION_CREDENTIALS in your environment
# to point to your service account JSON file
# Example: export GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account.json"
